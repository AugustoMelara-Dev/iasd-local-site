---
import Card from "../ui/Card.astro";

interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
---

<Card class="h-full">
  <h3 class="font-serif text-2xl text-ink-900">{title}</h3>
  <p class="mt-3 text-ink-600">{description}</p>
</Card>
